# ============================================================================
# Requirements for the full DFU tissue segmentation benchmarking pipeline
# (covers every script: run_supervised.py, run_mask2former.py,
# run_efficientnetb3_fpn.py, run_smp_model.py, run_all_combinations.py,
# visualize_all_models.py)
# ============================================================================

# ---- Core DL stack ----
torch>=2.1.0
torchvision>=0.16.0

# ---- Model backbones ----
timm>=1.0.0
# The real, unmodified segmentation-models-pytorch package -- used by
# run_smp_model.py, run_efficientnetb3_fpn.py, run_all_combinations.py, and
# visualize_all_models.py. This is DIFFERENT from the vendored fork shipped
# inside the DFUTissueSegNet repo's Codes/ folder (see note below).
segmentation-models-pytorch>=0.5.0

# The vendored fork's __init__ eagerly imports encoder families you won't
# use (resnet, efficientnet) even when only mit_b3 is needed, so these have
# to be present for run_supervised.py (the original MiT-B3+pscse baseline)
# even though the model itself only uses mit_b3.
pretrainedmodels>=0.7.4
efficientnet-pytorch>=0.7.1

# Mask2Former (run_mask2former.py) and its image processor.
transformers>=4.46.0
# Faster/more reliable Hugging Face Hub downloads (some checkpoints, e.g.
# SAM ViT-B, emit a warning without this and fall back to slower HTTP).
huggingface_hub[hf_xet]>=0.24.0

# ---- Data / augmentation / IO ----
albumentations>=1.3.1
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
scipy>=1.11.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

# ---- Notebook (optional, only if you want to run as .ipynb instead of .py) ----
jupyterlab>=4.0.0

# ============================================================================
# IMPORTANT NOTES
# ============================================================================
# 1. run_supervised.py (the original MiT-B3 + P-scSE baseline) imports the
#    MODIFIED fork shipped inside the DFUTissueSegNet repo itself
#    (Codes/segmentation_models_pytorch), NOT the pip package above -- that
#    fork adds the custom 'pscse' attention module the paper's model needs.
#    Do not let the two shadow each other: if you import both in the SAME
#    Python process (e.g. inside visualize_all_models.py, which needs to
#    reconstruct both model types), the module cache must be reset between
#    them -- already handled inside visualize_all_models.py.
#
# 2. run_all_combinations.py's SAM ViT-B + DPT combo needs a real download of
#    the actual SAM checkpoint (~360MB, via timm/Hugging Face Hub) on first
#    run. It also uses a partial-weight-transfer workaround for that
#    checkpoint's fixed-resolution position embeddings -- see the comment
#    above load_samvit_pretrained_partial() in run_all_combinations.py.
